import React from "react";

const SideCart = (props) => {
  const { id, imageSrc, title, price, oldPrice, rating, discount } = props.data;
  return (
    <>
      <div>SideCart</div>;
    </>
  );
};

export default SideCart;
